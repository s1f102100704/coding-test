const validParentheses = (str: string) => {
  const stack: string[] = [];
  const map = new Map<string, string>([
    [")", "("],
    ["}", "{"],
    ["]", "["],
  ]);
  for (const s of str) {
  }
};
